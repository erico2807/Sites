import React, { useState } from 'react';

function Punkte() {
    const [point, setPoint] = useState(0);
    

    return(
        <>
            <p>{point}</p> 
    </>
    )}
export default Punkte;
